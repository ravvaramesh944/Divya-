Nice — here's a single-file, unique HTML example that both looks nice and actually generates a unique code (UUID-like) you can copy. Paste this into a file named unique-code.html and open in your browser0
